# Copy to `.env` for local builds (never commit `.env`).
# Token is public (embedded in HTML); still better kept out of git if you prefer.

# Cloudflare Web Analytics site token
# Dashboard → Web Analytics → Manage site → JS snippet → token
PUBLIC_CF_ANALYTICS_TOKEN=
