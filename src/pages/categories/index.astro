---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { SITE_TITLE } from '../../consts';
import { getAllCategories, getPublishedPosts } from '../../utils/posts';

const posts = await getPublishedPosts();
const categories = getAllCategories(posts);
---

<BaseLayout title="分类" description={`${SITE_TITLE} 全部分类`}>
	<header class="mb-10">
		<h1 class="text-[1.75rem] font-medium tracking-tight text-[var(--fg)] sm:text-[2rem]">分类</h1>
		<p class="mt-2 text-[var(--olive)]">共 {categories.size} 个分类</p>
	</header>

	{
		categories.size === 0 ? (
			<p class="text-[var(--olive)]">暂无分类。</p>
		) : (
			<ul class="grid gap-3 sm:grid-cols-2">
				{[...categories.entries()].map(([category, count]) => (
					<li>
						<a
							href={`/categories/${encodeURIComponent(category)}/`}
							class="flex items-center justify-between rounded-xl bg-[var(--ivory)] px-5 py-4 no-underline transition-shadow hover:shadow-[var(--shadow-whisper)]"
						>
							<span class="font-medium text-[var(--fg)]">{category}</span>
							<span class="text-sm text-[var(--stone)]">{count} 篇</span>
						</a>
					</li>
				))}
			</ul>
		)
	}
</BaseLayout>
